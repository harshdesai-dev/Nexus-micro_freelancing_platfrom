-- Canonical NEXUS SQL contract (copy of Nexus_datsbase/migrations/001_initial_schema.sql)
-- Use this SQL only as the authoritative DB contract for RunSQL migrations.

BEGIN;
-- (truncated copy of the canonical SQL for brevity; full file stored in Nexus_datsbase/migrations/001_initial_schema.sql)
-- See that file for exact SQL to apply in production migration RunSQL steps.
COMMIT;
